-- generated by cds-compiler version 2.5.2
COLUMN TABLE app_expenses_Categories (
  name NVARCHAR(255),
  descr NVARCHAR(1000),
  code INTEGER NOT NULL,
  PRIMARY KEY(code)
) WITH ASSOCIATIONS (
  MANY TO MANY JOIN app_expenses_Categories_texts AS texts ON (texts.code = code),
  MANY TO ONE JOIN app_expenses_Categories_texts AS localized ON (localized.code = code AND localized.locale = SESSION_CONTEXT('LOCALE'))
)